#include <stdio.h>

int main()
{
    float radius = 7.0;
    float pi = 3.14159;
    float area = pi * radius * radius;
    printf("Area of Circle with radius %.4f is : %.4f\n", radius, area);
    return 0;
}